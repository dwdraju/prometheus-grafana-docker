---
route:
  group_by: [cluster]
  receiver: team-hipchat
  routes:
    - match:
      severity: hipchat
      receiver: team-hipchat

receivers:
 - name: team-hipchat

hipchat_config:
 - room-id: inputID
 - auth_token: inputTOKEN